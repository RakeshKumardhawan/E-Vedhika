# E-VEDHIKA Agent Instructions

1. **What's New Updates**: Do NOT update the `SYSTEM_UPDATES` array in `src/App.tsx` automatically. Only add a new entry when the user explicitly asks for it. When requested, use the sequence from V1.4.8 onwards, reflecting the changes with appropriate version numbers, date, badge, title, and description in Telugu.

2. Always ensure translations or new content is primarily in Telugu where applicable, matching the existing tone of the application.
3. Make sure to adhere to existing Tailwind design patterns and responsive breakpoints.
